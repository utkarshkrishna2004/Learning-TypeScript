const superhero: string[] = [];
const superpower: boolean[] = [];

const superheropoints: Array<number> = [];

type User = {
   name: string;
   isActive: boolean;
};

const allUsers: User[] = [];

superhero.push("Batman");
superpower.push(false);

superhero.push("Superman");
superpower.push(true);

allUsers.push({ name: "", isActive: true });

export {};
